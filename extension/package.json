{
  "name": "extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "esbuild background.js --bundle --outfile=background.bundle.js --format=esm && esbuild offscreen.js --bundle --outfile=offscreen.bundle.js --format=iife",
    "watch": "esbuild background.js --bundle --outfile=background.bundle.js --format=esm --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "@xenova/transformers": "^2.17.2",
    "esbuild": "^0.28.0"
  }
}
